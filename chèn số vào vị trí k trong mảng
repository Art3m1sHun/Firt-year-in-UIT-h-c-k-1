#include <iostream>
using namespace std;
#define MAX 100

void input(int a[], int &n){
	for (int i = 0; i < n; i++){
		cin>>a[i];
	}
}

int main() {
	int a[MAX];
	int n,x,k;
	cin>>n;
	input(a, n);
	cin>>x;
	cin>>k;
	 for (int i = n; i > k; --i) {
            a[i] = a[i - 1];
        }
	a[k] = x;
	for(int i = 0; i <= n; i++) cout<<a[i]<<" ";
	return 0;
	
}
