
#include <iostream>
using namespace std;

int reverse(int n) {
	int sum = 0;
	while (n > 0) {
		sum = (sum + (n%10)) * 10;
		n /= 10;
	}
	return sum/10;
}

int main() {
    int n;
    cin >> n;
    cout << reverse(n);
}
