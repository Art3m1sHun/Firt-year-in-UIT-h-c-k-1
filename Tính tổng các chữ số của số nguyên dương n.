
Đề bài:
Tính tổng các chữ số của số nguyên dương n.

#include <iostream>

using namespace std;

int main() {
  int so;
  int soTemp;
  int tong = 0;
  cin >> so;
  soTemp = so;
  if (so > 0){
  while (soTemp != 0) {
    tong += soTemp % 10;
    soTemp /= 10;
  }
}
  cout <<tong;
  return 0;
}
