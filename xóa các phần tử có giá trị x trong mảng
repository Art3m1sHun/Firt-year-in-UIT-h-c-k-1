#include <iostream>
using namespace std;
#define MAX 100

void input(int a[], int &n) {
    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }
}

int main() {
    int a[MAX];
    int n, x;
    cin >> n;
    input(a, n);
    cin >> x;

    for (int i = 0; i < n; i++) {
        if (a[i] == x) {
            for (int j = i; j < n - 1; j++) {
                a[j] = a[j + 1];
            }
            n--;
            i--; 
        }
    }

    for (int i = 0; i < n; i++) {
        cout << a[i] << " ";
    }
    return 0;
}
